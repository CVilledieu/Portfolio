// Code for the content of the page
function ContentStyle(color) {
    return (
        <style>{`
        #HudTitle {
            color: ${color};
        }
        `}</style>
    );
}

export default ContentStyle